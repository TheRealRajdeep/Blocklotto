// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

interface KeeperCompatibleInterface {
    /**
     * @notice Checks if upkeep is needed.
     * @param checkData Data passed from the keeper registry.
     * @return upkeepNeeded Boolean indicating whether upkeep should be performed.
     * @return performData Bytes that will be passed to performUpkeep.
     */
    function checkUpkeep(
        bytes calldata checkData
    ) external view returns (bool upkeepNeeded, bytes memory performData);

    /**
     * @notice Performs upkeep.
     * @param performData Data passed from checkUpkeep.
     */
    function performUpkeep(bytes calldata performData) external;
}
